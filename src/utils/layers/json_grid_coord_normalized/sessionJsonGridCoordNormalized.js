/** 「JSON·網格·座標正規化」圖層 id（示意網格／Pinia）；與 dataStore.layers 條目一致 */

export const LAYER_ID = 'json_grid_coord_normalized';

/** 程式內對此圖層之常用常數名（等同 {@link LAYER_ID}） */
export const JSON_GRID_COORD_NORMALIZED_LAYER_ID = LAYER_ID;
